name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Verificar que el pipeline corre
        run: echo "Pipeline de Aromas Store ejecutado correctamente"
